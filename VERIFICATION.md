# BIBU！测试与发布前验收指南

本文档定义 BIBU！的自动化测试套件规范以及在 Web 与 Android 端正式发布前必须执行的验收清单。

---

## 一、本地自动化测试套件

在任何代码修改或发版前，必须运行以下全部检查：

```bash
# 1. 静态类型检查
npm run typecheck

# 2. 核心单元与集成测试（包含 PGlite 数据库 RLS/RPC、IndexedDB 缓存、原生桥、日期等）
npm test

# 3. 生产环境构建检查
npm run build

# 4. 代码格式与规范检查
npm run format:check

# 5. Android 发布门禁检查（检查签名变量、通知图标等）
npm run android:release:check
```

---

## 二、核心业务场景验收清单

### 1. 账号认证与双人专属绑定

- [ ] **魔法链接登录**：输入邮箱获取 Magic Link，验证通过后进入首页并自动建立 profile。
- [ ] **原子空间创建与邀请码**：A 用户创建空间，生成 24 小时有效的随机邀请码。
- [ ] **双人绑定**：B 用户输入邀请码成功加入，双方界面同步刷新进入小窝。
- [ ] **第三人严格排他**：C 用户尝试使用已消费的邀请码加入，系统拒绝并明确提示空间已满。
- [ ] **解除关系与封存**：在空间设置中确认解除关系，空间标记为封存，历史数据隔离且不再可加入。

### 2. 双向情感 Ping 与通知

- [ ] **六种情绪发送**：测试“哔卟哔卟”、“想你”、“抱一下”、“快来”、“晚安”、“我回来啦”，验证点击震动与方波音效。
- [ ] **服务端 3 秒行锁防刷**：3 秒内连续点击 Ping 按钮，服务端拒绝多余请求并提示冷却。
- [ ] **专注防打扰联动**：
  - 对方未开启专注：正常接收提醒。
  - 对方开启专注但未授权：发送 Ping 时服务端拦截并明确提示对方专注中。
  - 对方开启专注且授权：允许发送。

### 3. 私密聊天与共同记忆

- [ ] **即时消息发送**：支持中英文、换行及原生 Emoji，中文输入法组合键回车不发生误发。
- [ ] **离线与断网恢复**：断网状态下发送消息，消息进入本机待发队列并标红；恢复网络后支持手动重试或自动同步。
- [ ] **照片上传与签名链接**：上传图片生成私有 Storage 存储，并在详情中正常渲染签名图片。
- [ ] **本地持久缓存**：已加载照片在离线状态下仍能通过 IndexedDB 读取本地 Blob 正常展示。

### 4. 萌宠衣橱与视觉适配

- [ ] **16 款小动物切换**：可正常切换所有 16 款基础角色立绘。
- [ ] **装备穿戴与兼容性**：不兼容单品在列表正确置灰显示并提示，兼容单品穿戴后无悬空或穿模。

---

## 三、Android 原生能力验收清单

### 1. 原生通知与渠道

- [ ] **通知渠道建立**：应用初次启动后，系统通知设置中存在“两个人的哔卟”与“BIBU！悄悄话”两个高优先级私密渠道。
- [ ] **状态栏图标**：触发通知时，状态栏与通知横幅正确使用专用小图标（`ic_stat_bibo`）。
- [ ] **通知点击回跳**：点击消息通知后，唤醒应用并正确通过 Deep Link 跳转至对应聊天消息。

### 2. 定时小约定 (AlarmManager)

- [ ] **非精确定时提醒**：设置 1 分钟后的约定提醒，退到手机桌面或锁屏，到期后系统准时弹出提醒。
- [ ] **系统重启恢复**：已设置的提醒在系统重启或升级后由广播接收器重新注册。

### 3. 屏幕使用时长 (UsageStats)

- [ ] **权限申请引导**：点击专注页使用情况时，引导跳转系统“使用情况访问权限”页面。
- [ ] **数据本地读取**：授权后能在本机准确展示今日屏幕使用时长或专注时长，且数据不外传。

### 4. 发布构建与 APK 验收

- [ ] **Release 构建通过**：配置签名环境变量后执行 `npm run android:release:build` 成功生成 Release APK。
- [ ] **签名有效性核验**：使用 `apksigner verify --print-certs` 确认 V2 签名无误。

---

## 四、生产托管环境验收清单

- [ ] **Vercel 生产部署**：生产域名配置 HTTPS 且正确反向代理，无公开暴露私密 Service Role 密钥。
- [ ] **Supabase Webhook 端到端打通**：`messages` 与 `pings` 插入记录时，触发器成功调用 Edge Function。
- [ ] **个推远程真实送达**：在已配置个推的物理设备间完成后台远程推送真实送达验收。
