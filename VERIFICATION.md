# 第一阶段验证记录

验证日期：2026-09-07。范围：本地 checkout、生产构建、本地演示浏览器、PGlite PostgreSQL 数据库执行。没有访问用户的 Supabase / Vercel 账号。

## 已完成

| 验证                                             | 结果          |
| ------------------------------------------------ | ------------- |
| TypeScript 类型检查、Vite 生产构建               | 通过          |
| Vitest 日期 / 图片输入测试                       | 7 项通过      |
| PGlite 迁移及 RLS / RPC 测试                     | 13 项通过     |
| 聊天文字发送、刷新后本地持久化、Emoji 组合       | 浏览器通过    |
| 共同倒计时新建、确认删除                         | 浏览器通过    |
| 图片选择、演示上传、大图查看、Esc 关闭           | 浏览器通过    |
| 专注开始、计时、提前结束                         | 浏览器通过    |
| 专注提醒默认未勾选、伴侣未授权时按钮禁用         | 浏览器通过    |
| 昵称保存、声音开关主动授权                       | 浏览器通过    |
| 哔卟接收特效和确认关闭                           | 浏览器通过    |
| 320 / 390 / 768 / 1024 / 1440 宽度，六页横向溢出 | 30 个组合通过 |
| 上述浏览器过程的 JavaScript pageerror            | 未发现        |

截图及本次浏览器运行记录放在被 Git 忽略的 `output/playwright/`。测试使用独立浏览器演示数据；结束时移除了测试演示记录，页面恢复默认示例。

**声音开关成功不等于实测听觉效果；桌面浏览器验证不等于 Android / iOS 震动已经实测。**

## 数据库测试具体覆盖

- Auth 触发器自动创建 profile。
- 创建空间和成员记录；只能创建一个空间。
- 未授权直接写成员、读邀请码表被拒绝。
- 第二位玩家通过邀请码加入；消费后第三位玩家无法加入。
- 满员空间不能生成第三位邀请码。
- 两个独立空间的聊天 / 日期 / 照片访问隔离。
- 不能伪造发送者；不能自行指定消息时间。
- 本人可以更新本人昵称，不能更新伴侣昵称。
- 伴侣没有专注或未授权时，服务端拒绝相应提醒。
- 不能替伴侣开启提醒许可；本人授权后可以发送。
- 哔卟 3 秒冷却及禁止直接写 pings。
- Storage 桶非公开，目录读取与上传 / 删除身份约束。
- 新邀请码使旧码失效，过期邀请码被拒绝。
- 匿名角色不能调用业务 RPC 或读取私密表。

这里运行了 PostgreSQL 引擎中的真实 SQL，但 auth / storage 系统对象是最小替身，不是 Supabase 托管服务本身。未开展真实多连接并发测试。

## 连接 Supabase 后必须完成的验收

使用独立测试项目，不要向演示中粘贴真实密钥或上传敏感资料。

1. **双账号登录**：A 与 B 使用不同邮箱、独立浏览器登录；确认 SQL 触发器生成 profile，魔法链接回到正确环境。
2. **绑定**：A 创建空间，在设置复制邀请码；B 输入并加入；两端头像 / 昵称更新。
3. **第三人隔离**：C 不属于该空间；使用已消费码加入失败，对同空间表 / Storage 的读取被拒绝。
4. **并发加入**：两个独立连接尝试使用同一邀请码；最多一个成功，空间仍只有两位成员。
5. **实时聊天**：A 输入中文、Emoji、多行文本；B 不刷新看到更新；断网后界面提示连接异常，恢复后补齐聊天。
6. **真实照片**：A 上传 JPEG；B 能查看大图。匿名请求不能直接读取对象；签名链接可访问但会过期。
7. **提醒**：B 页面前台开启声音偏好；A 点击哔卟；B 出现特效。分别检查 Android Chrome 声音和震动，记录不支持的平台。
8. **专注授权**：B 开始专注但不勾选许可，A 的提醒应被服务器拒绝；结束后重新开启并授权，提醒恢复。A 不能改变 B 的许可。
9. **页面挂起**：锁屏 / 关闭网页后不应声称保证送达；当前 UI 和文档保留限制说明。
10. **生产构建**：Vercel 正确注入公开环境变量，重新构建后登录回调到正式 HTTPS 域名；不要把管理员 key 注入构建。
11. **照片失败路径**：模拟上传成功但元数据登记失败，确认清理结果真实反馈；不要显示假成功。
12. **时区与纪念日**：确认双方对自然日和周年日期的时区预期，测试 2 月 29 日、今天、跨年和过去日期。

## 尚未交付

线上 Supabase 部署、Vercel 线上地址、实际双设备送达、后台推送、Android 工程与 APK、已读回执、端到端加密、解绑 / 注销 / 数据导出、自动 EXIF 移除和旧历史分页。这些均不应被本地测试结果替代。

## 纪念日 / BIBO 底栏增量验证

本轮最终构建与 34 项自动化测试通过，其中新增 14 项 SVG 图标兼容 / 白名单 / 渲染检查。

浏览器已验证：

- 里程碑按真实在一起日期计算，旧事件正常显示为 SVG。
- 新建事件选择小狗，保存 `icon:dog`，刷新后保持选择。
- 筛选纪念日 / 倒计时及确认删除仍然可用。
- 更多面板中的照片墙、专注陪伴、设置全部可达，支持 Esc 和点击外部关闭。
- 在设置页直接点击中间 BIBO 可以触发提醒；32 个粒子全部为 SVG。
- 哔卟发送后切到首页，首页按钮和底栏按钮共享冷却，不能绕过。
- 六页面在 320 / 390 / 760 / 768 / 1024 / 1440 宽度共 36 个组合无横向溢出。
- 手机底栏中心按钮几何中心与视口中心一致；320 宽度下 12 项 SVG 选择器无横向溢出。
- 上述过程没有 JavaScript pageerror；测试结束恢复该测试浏览器原有的演示记录。

使用 `pixelarticons@2.4.1`，实测包文件逻辑大小 3,108,078 字节，五枚直接导入 SVG 总源码 1,283 字节；没有导入整库或额外图标字体。该统计是文件体积，不是运行内存测量。

增量截图和记录：`output/playwright/events-v2-desktop.png`、`events-v2-mobile.png`、`event-picker-v2.png`、`revision-smoke-result.txt`。

## 图标选择器扩充验证

当前最终状态：40 个图标、8 个动物选项、五类主题（另有全部入口）和关键词搜索。自动化测试共 **65 项通过**，生产构建通过。

浏览器验收：

- 确认 40 个选项、8 个小动物；所有分类有对应图标。
- 搜索“小熊”相关关键词、查无结果后清除筛选；筛选期间原有选择保持不变。
- “考试”匹配读书和目标图标；搜索框 Enter 不提交事件表单。
- 导入 SVG 均成功解码，自绘图形不是空 SVG。
- 320 / 390 / 760 / 1440 宽度下弹窗没有横向溢出，图标网格最后一项可滚动到并选中。
- 选择新企鹅图标、创建期待、刷新后仍以 `icon:penguin` 保存并显示；测试后恢复原演示数据。

截图：`output/playwright/picker-friends-40-mobile.png`。浏览器记录：`output/playwright/icon-expansion-result.txt`。本轮无需新 SQL 或后端权限变更，也未修改用户提供的参考 demo。
