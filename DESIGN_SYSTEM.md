# BIBU 设计系统

## 视觉方向

两个人专属的像素游戏小基地。提取参考图中的高饱和彩色分区、粗黑框、像素文字、硬阴影与游戏式即时反馈，不照搬图片和水印。不使用 SaaS 模板、渐变玻璃、Material 圆卡或霓虹暗色赛博风。

## 统一 tokens

| Token  | 色值      | 用途                       |
| ------ | --------- | -------------------------- |
| Ink    | `#20211d` | 正文、描边、硬阴影         |
| Paper  | `#fffef7` | 面板、对话与阅读区域       |
| Yellow | `#fff238` | 主按钮、当前导航、重要提示 |
| Blue   | `#04bcf0` | 在一起天数主面板           |
| Green  | `#2aeea4` | 专注、成功反馈、本人消息   |
| Pink   | `#ffa6e8` | 哔卟区、伴侣消息、互动反馈 |

背景为奶白和 24 像素淡网格。卡片 2 像素黑描边、3–4 像素无模糊偏移阴影。照片有轻微倾斜，悬停归正。边角以直角为主。

## 字体与阅读

- Press Start 2P 用于品牌、天数、计时器和英文装饰标签，字体随构建本地加载。
- 中文标题和正文使用系统中文字体。中文不依赖远程字体请求。
- 小号英文只做装饰，不承担理解或操作的必要信息。
- 实际操作均有中文文字或可访问名称，图标 SVG 对辅助技术隐藏。

## 共用组件

`Button`：黄 / 绿 / 粉 / 蓝 / 白 / 黑六种色调，统一边框与按压位移，禁用和待处理状态。

`Panel`：一致的内容标题栏、边框和间距。

`Modal`：原生 dialog，支持焦点约束、Esc、关闭按钮和恢复滚动。危险操作需明确确认。

`Icon` / `PixelPal` / `PixelFlower`：本地矢量像素组件，统一使用 crispEdges。

`EventCard`：首页和日期页复用同一倒计时组件。

`PhotoCard` / `PhotoViewer`：照片墙与首页共享照片展示和放大。

`Shell`：桌面侧栏、平板窄栏、手机底部导航共享页面状态。

## 响应式

- 760 像素及以下：手机布局；底部五项导航，资料按钮进入设置。
- 761–1020 像素：图标窄侧栏。
- 1021 像素及以上：完整侧栏和双列首页。
- 内容区域限制最大宽度，宽屏不会无限拉长。
- 底部导航考虑安全区；全屏提醒使用动态视口高度。

## 动画规则

按钮是短促硬位移，页面入场采用 steps 分步动画，哔卟为方波提示音、像素跳动和全屏 Emoji 雨。

尊重 `prefers-reduced-motion`：停止自动雨落和跳动，用静态特效保留反馈。声音和震动默认关闭，必须由本人点击启用。

## 下一阶段

先在真实双机上确认交互强度，再调声音响度、提醒频率和动效密度。不要用更多动画换取“像素感”，也不要把装饰字体扩展到长中文消息。

## 纪念日与底栏增量调整

参考用户本地 `pixel-love---情侣双人空间 (1)` 中的纪念日、底栏组件，仅调整相关区域，不迁移其全局布局或业务逻辑。

- 纪念日页加入真实恋爱天数计算的 100 / 365 / 520 / 1000 天徽章、彩色分类标签和大号倒计时。
- 装饰使用 SVG：自绘送信小兔、出游小狗、蛋糕、海浪、列车、飞机和小树，搭配少量 Pixelarticons。
- 新事件使用 64 种多色彩色 SVG 选择项（五类筛选与关键词搜索）；选自库中图形均加入专属多层复古色彩，告别单一黑色剪影；旧事件 Emoji 字段在读取时转换为图形，不批量改写历史资料。
- 手机底栏为「小窝 / 悄悄话 / BIBO / 值得期待 / 更多」。BIBO 居中凸起，更多明确列出照片墙、专注陪伴和设置，不做隐式循环切页。
- 首页与底栏共用同一个哔卟操作及冷却状态；提醒粒子也使用 SVG。原有聊天 Emoji 输入功能和用户消息不变。
- 增量样式单独放在 `src/expectations.css`，桌面侧栏及其他页面布局保持原样。
