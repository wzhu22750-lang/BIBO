# BIBU！设计系统规范

## 一、设计理念与视觉方向

BIBU！是两个人专属的像素游戏小基地。以高饱和像素色彩、粗黑描边、复古像素字体、硬边缘无模糊投影与即时游戏交互反馈为核心，呈现温暖、有趣、专属的复古游戏感。

- **不使用**：泛滥的 SaaS 圆角卡片、毛玻璃模糊、复杂渐变或冷淡极简风格。
- **坚持**：直角与 2px/4px 阶梯式像素几何、短促硬位移按压动画、全矢量 SVG 绘制。

---

## 二、统一设计 Tokens

| Token      | 变量名称   | 色值      | 核心用途                         |
| ---------- | ---------- | --------- | -------------------------------- |
| **Ink**    | `--ink`    | `#20211d` | 正文字色、粗外描边、硬投影       |
| **Paper**  | `--paper`  | `#fffef7` | 背景底纸、面板区域、输入框背景   |
| **Yellow** | `--yellow` | `#fff238` | 主品牌色、核心操作按钮、选中状态 |
| **Blue**   | `--blue`   | `#04bcf0` | 纪念日看板、恋爱天数、冷色系点缀 |
| **Green**  | `--green`  | `#2aeea4` | 专注计时、成功状态、本人消息气泡 |
| **Pink**   | `--pink`   | `#ffa6e8` | 伴侣消息气泡、情绪反馈、爱心点缀 |
| **Cream**  | `--cream`  | `#f7f4e9` | 页面次级底色、复古浅网格背景     |

---

## 三、排版与字体系统

- **英文字体**：采用本地随构建打包的 `Press Start 2P`，专用于品牌 Logo、恋爱天数、计时器及小号标签装饰。
- **中文字体**：正文与中文标题采用系统高可读性无衬线字体（PingFang SC / Microsoft YaHei / sans-serif），确保长文本与中文聊天的最佳阅读体验。
- **无障碍访问**：所有图标与图形操作均具备清晰的中文语义或 `aria-label`；视觉装饰元素显式设置 `aria-hidden="true"`。

---

## 四、核心通用组件

- **Button**：提供 Yellow / Blue / Green / Pink / White / Black 等像素配色，统一 2px 黑描边与 3px 偏移投影，具有清脆按压位移效果。
- **Panel**：像素游戏风格的内容卡片容器，统一头部装饰带、粗边框与阴影。
- **Modal**：基于原生 `<dialog>` 封装，支持焦点捕获、Esc 键安全关闭、禁止背景滚动。
- **PixelArt & Icons**：包含 16 款复古像素小动物、64 款纪念日生活场景全彩 SVG，全部采用 `crispEdges` 渲染，避免像素模糊。
- **BottomNav**：移动端底部导航栏，中间凸起核心 BIBU！情绪按钮，支持单击发射与长按浮动选择情绪。

---

## 五、响应式与设备适配

- **移动端（<= 760px）**：底部常驻五项导航栏，适配刘海屏与系统手势条 `env(safe-area-inset-*)`。
- **平板与中等屏幕（761px - 1020px）**：收起为紧凑型左侧图标栏。
- **桌面端（>= 1021px）**：展现完整桌面侧边栏与双列小窝布局。
- **系统偏好感知**：尊重用户 `prefers-reduced-motion` 设置，开启时停用高频粒子与跳动动画，降级为静态提示。
